function root(input: string = ""): object {
  // let root = "../../";
  // if (input[0] !== "/") {
  //   root += "/";
  // }
  // return root + input;

  return { root: "./" };
}

export default root();
